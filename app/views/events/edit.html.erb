<div class="container">
  <div class="row">
    <div class="col-md-6 offset-md-3">
      <br><br><br>
      <%= form_tag event_path(@event.id), html: { class: "form-signin mt-3" }, method: "put", multipart: true do %>
        <h1 class="h3 mb-3 font-weight-normal text-center">Modifier l'événement <%= @event.title %> </h1>
        <p>Titre: </p>
        <%= text_field_tag 'title', @event.title, class: 'form-control' %>
        <p>Description: </p>
        <%= text_area_tag 'description', @event.description, class: 'form-control' %>
        <p>Illustration: </p>
        <%= file_field_tag :image %>
        <p>Date: </p>
        <%= datetime_local_field_tag 'start_date', @event.start_date, class: 'form-control' %>        
        <p>Durée (en minute par palier de 5 minutes): </p>
        <%= number_field_tag 'duration', @event.duration, class: 'form-control', in: 0..1440, step: 5 %>
        <p>Ville: </p>
        <%= text_field_tag 'location', @event.location, class: 'form-control' %>
        <p>Prix (entre 1 et 1000€): </p>
        <%= number_field_tag 'price', @event.price, class: 'form-control', in: 1..1000, step: 1 %>
        <br>
        <%= submit_tag "Envoyer",  class: "btn btn-lg btn-primary btn-block" %>
      <% end %>
    </div>
  </div>
</div>
