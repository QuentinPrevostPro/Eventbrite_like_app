<nav class="navbar navbar-expand-lg navbar-light bg-light">
      <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
        <div class="navbar-nav">
          <span class="nav-item nav-link active"> <%= link_to "Eventbrite", events_path %> </span>
          
          <% if user_signed_in? %>
            <div class="btn-group dropright" style="margin-right: 670px">
              <button type="button" class="btn btn-secondary dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">Mon profil</button>
              <div class="dropdown-menu">
                <span class="dropdown-item"> <%= link_to "Accéder à mon profil", user_path(current_user) %> </span>
                <span class="dropdown-item"> <%= link_to "Se déconnecter", destroy_user_session_path, method: "DELETE" %> </span>
              </div>
            </div>
            <% if current_user.is_admin %>
              <%= link_to "Portail administrateur", admin_events_path, class: "btn btn-secondary" %>
            <% end %>
          <% else %>
            <div class="btn-group dropright">
              <button type="button" class="btn btn-secondary dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">S'inscrire / Se connecter</button>
              <div class="dropdown-menu">
                <span class="dropdown-item"> <%= link_to "S'inscrire", new_user_registration_path %> </span>
                <span class="dropdown-item"> <%= link_to "Se connecter", new_user_session_path %> </span>   
              </div>
            </div>
          <% end %>
        </div>
</nav>